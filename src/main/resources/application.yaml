server:
  port: 2014

spring:
  application:
    name: product-service

  datasource:
    #url: jdbc:mysql://localhost:3306/product_db
    url: jdbc:mysql://localhost:3306/store_db
    username: root
    password: ellenverma
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
